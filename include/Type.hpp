#ifndef TYPE_H
#define TYPE_H

// Lets rename double as real in the assignment
typedef double real;

typedef std::vector<std::map<size_t,double>> Mat;

//typedef std::vector<std::map<size_t, double> > Mat;

#endif
